# Session Summary

*Tóm tắt trạng thái làm việc GẦN NHẤT — đọc ngay sau [CLAUDE.md](../../CLAUDE.md) mỗi phiên mới hoặc sau `/compact` (xem "AI Startup Workflow" trong CLAUDE.md). Khác [TASKS.md](./TASKS.md): TASKS.md là checklist lâu dài theo tính năng, file này là nhật ký ngắn "vừa làm gì / đang chờ gì" — có thể lỗi thời nhanh, ai cập nhật sau thì ghi đè thẳng, không cần giữ lịch sử cũ.*

## Sprint gần nhất (đã code xong, đang chờ user tự kiểm thử tay)

1. **Map Selection System (Phase 1)** — data-driven 10 map (Forest/Graveyard có nội dung thật, 8 map còn lại placeholder), `MapSelectScene` + `MapRenderer` (world-map dạng đảo liên kết), `MapData.ts` làm repository. GameScene/SpawnSystem/BossSystem nhận map qua `mapId`, không hardcode.
2. **Victory Flow & Boss Death Cinematic rework** — xoá cutscene cũ, xây lại qua `VictoryController` (Zoom Camera → Slow Motion 25% → Boss tan biến → Restore TimeScale) + `VictoryScene` mới (tách khỏi GameOverScene, chỉ còn xử lý thua trận). "Map tiếp theo" giờ đi qua `MapSelectScene` (map mới tự chọn sẵn), không vào thẳng GameScene.
3. **UX & Gameplay Improvements (Phase 1)** — 5 tính năng: Pause hiện tên map, Boss HP dạng `current/max` realtime, Card Selection redesign (Icon+Level+Description đọc từ data, không hardcode text), Weapon HUD tray (tự render từ `player.equippedWeapons`, dùng chung `WeaponIcon.ts` với Card).
4. **Thử Thách 7 Ngày** — feature mới, data-driven qua `data/challengeDays.json` (7 ngày, MỖI NGÀY 3 mission `kill_enemies` cùng chia sẻ 1 bộ đếm luỹ tiến theo ngày) + `data/challengeMilestones.json` (4 mốc sao: 7/14/21/28, max 28 sao = 7 ngày × 4 sao/ngày). `Challenge7DaysManager` (src/systems/) quản lý state trong `localStorage` (key `soulhunter_challenge7days_state`), ngày mở TUẦN TỰ khi TẤT CẢ mission trong ngày hoàn thành. `Challenge7DaysScene` popup 3 cột 900x500, icon vector (`ui/ChallengeIcons.ts`) thay emoji Unicode. Tiến độ cập nhật qua `GameScene.registerKill()` → `Challenge7DaysManager.addKillProgress()`. 3 event EventBus: `CHALLENGE_PROGRESS_UPDATED`, `CHALLENGE_DAY_UNLOCKED`, `CHALLENGE_REWARD_CLAIMED`.
5. **Fix 4 vấn đề UX Thử Thách 7 Ngày** (theo yêu cầu user, đã test qua Browser xác nhận từng điểm):
   - **Chọn ngày khó bấm** — tăng hit area `ChallengeDayItem` vượt ra ngoài khung nhìn thấy (`HIT_PADDING`), chọn kích hoạt lúc `pointerup` (không phải `pointerdown`) để có hiệu ứng nhấn (scale 0.96) rõ ràng trước khi popup render lại, viền/glow vàng đậm rõ rệt cho trạng thái Selected (phân biệt với viền xanh của trạng thái Current).
   - **Thanh sao chạy sai chiều** — viết lại `ChallengeProgressBar.ts`: bỏ hẳn cách cũ dựa vào `setOrigin` để tự mọc theo 1 chiều (dễ vỡ khác nhau giữa Canvas/WebGL renderer), giờ mỗi lần update tự tính lại CẢ size lẫn x/y của rectangle fill — neo mép dưới cố định, fill mọc lên trên, đảm bảo đúng hướng bất kể renderer.
   - **Phần thưởng vũ khí gây hiểu lầm** — `ChallengeMilestoneDef` thêm field `rewardType: "coin" | "weapon_unlock" | "permanent_upgrade_token"`, `claimMilestone()` giờ CẤP THẬT (tái dùng `SaveData.unlockWeapon()`/`getCheapestLockedWeapon()`/`addPermanentUpgradeTokens()` — không phát minh hệ thống mới). `ChallengeMilestoneState` thêm state thứ 4 `"owned"` (riêng `weapon_unlock` đã claim) hiển thị TÊN THẬT vũ khí vừa mở khóa (`Challenge7DaysManager.getMilestoneRewardLabel()`) + tag "ĐANG SỞ HỮU" màu xanh riêng biệt, khác hẳn "claimed" (Coin/Token, tích xanh lá). Milestone "Character Item" giả (không có hệ thống backing thật) đã bị XOÁ khỏi data, thay bằng `+1 Permanent Upgrade Token` (resource có thật).
   - **Reward Milestone chỉ có icon** — `rewardName` trong data giờ là text ĐẦY ĐỦ rõ số lượng (`"100 Coin"`, `"250 Coin"`, `"+1 Permanent Upgrade Token"`), `ChallengeRewardMilestone.ts` hiển thị label này (wordWrap 2 dòng) thay vì chỉ icon.
6. **Fix tiếp 2 vấn đề UX Thử Thách 7 Ngày** (đã test qua Browser xác nhận từng điểm):
   - **Text reward tràn khung** — `MILESTONE_LABEL_MAX_WIDTH` ở `Challenge7DaysScene.ts` giờ tính đúng bằng khoảng trống THẬT tới mép panel (trước đó hằng số `LABEL_WRAP_W` hardcode rộng hơn không gian thực tế còn lại, khiến "Mở Khóa Vũ Khí Đặc Biệt" tràn ra ngoài panel). Thêm `clampToLines()` trong `ChallengeRewardMilestone.ts` — dùng `Text.getWrappedText()` để CHỦ ĐỘNG cắt còn tối đa 2 dòng + "..." nếu reward name tương lai dài hơn nữa, đảm bảo không bao giờ tràn dù data đổi.
   - **Thanh Tổng sao animate lại mỗi lần đổi ngày** — tách `Challenge7DaysScene` thành 3 layer render độc lập: `dayListLayer`/`missionLayer` (rebuild khi đổi `selectedDay`) và `starBar`/`starInfoLayer` (KHÔNG đụng khi đổi ngày). `starBar` (`ChallengeProgressBar`) giờ là 1 instance field tạo DUY NHẤT 1 lần trong `create()`, không còn bị destroy+recreate mỗi lần render lại — chỉ gọi `.setProgress()` khi tổng sao THỰC SỰ đổi (`CHALLENGE_PROGRESS_UPDATED`). Claim milestone (`CHALLENGE_REWARD_CLAIMED`) chỉ rebuild `starInfoLayer` (nhãn tổng sao + milestone rows), không đụng bar vì claim không đổi tổng sao.

7. **Tính năng Collection mới** — màn xem lại Monster/Weapon/Boss/Card đã khám phá, mở từ nút góc trên-phải MenuScene:
   - **Data**: KHÔNG tạo `monsters.json`/`weapons.json`/`bosses.json`/`cards.json` như đề bài liệt kê ban đầu — `weapons.json`/`bosses.json` đã là data gốc cho gameplay (WeaponSystem/BossSystem đọc trực tiếp), tạo file trùng tên sẽ phá nguyên tắc "1 nguồn dữ liệu duy nhất". Thay vào đó `CollectionManager` (systems/) tổng hợp lại từ đúng các file data thật đã có (`enemies.json` + `enemies_graveyard.json` dedupe theo id, `weapons.json` + `fusionWeapons.json`, `bosses.json` + `bossSkills.json`, `upgrades.json`), chỉ bổ sung field hiển thị còn thiếu trực tiếp vào các file đó: `rarity` (weapons/fusionWeapons/upgrades), `description` (enemies/enemies_graveyard/bosses), `name` (bossSkills — tên kỹ năng hiển thị).
   - **1 hệ thống Card thống nhất**: `ui/CollectionCard.ts` (mới) là component Card DUY NHẤT của toàn game — nền nhiều lớp bo góc (Graphics, không phải Rectangle phẳng), viền màu theo rarity (Common/Rare/Epic/Legendary, bảng màu `RARITY_COLORS`), silhouette "?????"/"Chưa khám phá." khi khoá. `LevelUpScene` (màn chọn Upgrade lúc nhặt Soul) đã refactor dùng thẳng component này qua `ui/CardContentBuilders.ts` (map `CardData` gameplay -> `CardContent` hiển thị) — **đã xoá `ui/LevelUpCard.ts`**. `PauseScene` (tray loadout mini-card) giữ nguyên hành vi cũ, chỉ đổi import: phần style theo trạng thái "mới/nâng cấp/fusion" (khác khái niệm rarity) tách riêng vào `ui/CardStyle.ts`.
   - **UI**: `CollectionScene` popup 2 khu vực — trái `CollectionTab` (4 tab, viền/glow vàng khi chọn), phải grid `CollectionCard` scale 0.62 (dùng chung component, chỉ scale nhỏ lại) + scrollbar tự hiện khi nội dung dài hơn khung (Weapons 22 mục, Cards 21 mục), scroll bằng wheel với mask Graphics giới hạn viewport.
   - **Bug pointerup**: cơ chế "chọn lúc pointerup" (giống pattern `ChallengeDayItem`) KHÔNG nhận event ổn định trong môi trường Browser pane test — pointerdown vào nhưng pointerup không bao giờ fire. Đã sửa `CollectionTab`/`CollectionCard` (`onSelect`) chuyển hẳn sang chọn lúc **pointerdown**, khớp mọi nút khác của game (Menu, nút đóng popup).
8. **Fix Collection: tab Boss/Cards không phản hồi + đưa unlock về CollectionManager + real-time sync** (theo yêu cầu user, đã test qua Browser xác nhận):
   - **Nguyên nhân tab không phản hồi**: `CollectionScene.selectTab()` cũ gọi `renderTabs()` MỖI LẦN đổi tab — destroy toàn bộ 4 `CollectionTab` (kể cả tab đang được click) rồi tạo lại NGAY GIỮA lúc xử lý sự kiện pointerdown của chính tab đó. Hành vi undefined/không ổn định tuỳ trình duyệt — đúng dạng bug chỉ xuất hiện lúc thật (không phải mọi lần test đều tái hiện được). Fix: `CollectionTab` giờ có `setSelected(selected)` cập nhật màu/viền/glow TẠI CHỖ; `CollectionScene` tạo cả 4 tab DUY NHẤT 1 lần trong `create()` (mảng `tabs`), `selectTab()` chỉ gọi `setSelected()` trên tab cũ/mới + render lại grid bên phải — không bao giờ destroy tab nữa.
   - **CollectionManager là điểm ghi DUY NHẤT**: thêm 4 hàm `unlockMonster/unlockWeapon/unlockBoss/unlockCard(id): boolean` — tự kiểm tra đã unlock chưa (tránh ghi trùng), gọi `utils/CollectionSaveData.ts` để lưu, rồi bắn `GameEvents.COLLECTION_UNLOCKED {type, id}` NẾU thực sự mới unlock. `WeaponSystem`/`UpgradeSystem`/`FusionSystem`/`BossSystem` đổi sang gọi `CollectionManager.unlockX(id)` thay vì gọi thẳng `discoverX()` từ `CollectionSaveData.ts` như trước (file đó giờ coi như private, chỉ `CollectionManager` được import).
   - **Real-time sync**: `CollectionScene` lắng nghe `GameEvents.COLLECTION_UNLOCKED` — nếu popup đang mở VÀ tab vừa unlock trùng tab đang xem, chỉ `renderGrid()` lại (giữ nguyên vị trí scroll, không đụng tab/không reload cả popup). Hiện tại Collection chỉ mở được từ MenuScene (không song song với GameScene) nên tình huống này chưa xảy ra trong luồng chơi thực tế, nhưng cơ chế đã sẵn sàng nếu sau này Collection mở được lúc pause/gameplay.
9. **Fix Collection: vùng click Tab quá nhỏ + fill đầy đủ data + tạm unlock hết để dev test** (theo yêu cầu user, đã test qua Browser xác nhận):
   - **Nguyên nhân thật của "vùng click nhỏ"**: `CollectionTab`/`CollectionCard` gắn `setInteractive()` trực tiếp vào `Container` kèm `new Phaser.Geom.Rectangle(0,0,W,H)` tính tay — toạ độ đúng trên giấy (đã verify lại bằng công thức) nhưng THỰC TẾ chỉ nhận click ở khoảng NỬA TRÁI của vùng, nửa phải hoàn toàn không phản hồi. Xác nhận bằng cách click nhị phân nhiều điểm x khác nhau trong cùng 1 tab (x=48/60/104 ăn, x=115/130/155/163 không ăn dù vẫn nằm trong khung nhìn thấy) — Container + custom Geom.Rectangle hitArea là pattern KHÔNG đáng tin cậy trong môi trường test này (đã dùng lại pattern này ở `ChallengeDayItem` trước đó mà chưa từng bị phát hiện vì test trước đó chỉ click gần tâm).
   - **Fix**: bỏ hẳn custom hitArea trên Container — `CollectionTab` gắn `setInteractive()` (không tham số) thẳng vào `this.bg` (Rectangle nền có sẵn) để Phaser tự tính hit area đúng theo width/height/origin thật. `CollectionCard` thêm 1 Rectangle `hitZone` riêng (alpha≈0, đúng CARD_W x CARD_H) làm vùng click thay vì Container hitArea. Cả 2 đã test lại bằng cách click đúng những điểm trước đó fail (x=163 — mép phải mọi tab) — ăn hết.
   - **Bug phụ phát hiện lúc fix**: hover trong grid Collection làm card BUNG VỀ FULL SIZE (190x250) đè lên cả cột tab bên trái — do `CollectionCard`'s hover tween set `scale` TUYỆT ĐỐI (1.05) trên cùng Container mà `CollectionScene` đã set `scale = GRID_SCALE (0.62)` để thu nhỏ card vào lưới, 2 chỗ ghi đè lẫn nhau. Thêm `CollectionCard.setBaseScale(scale)` — hover/click giờ animate quanh `baseScale` thay vì giá trị tuyệt đối; `CollectionScene` gọi `card.setBaseScale(GRID_SCALE)` thay vì set scale trực tiếp. Đây chính là 1 dạng "object con chặn input" mà bug report nhắc tới — card ballooned có thể che khuất/chặn click vào tab.
   - **Fill data + Dev unlock**: dữ liệu Collection ĐÃ đầy đủ từ trước (Monsters 10, Weapons 22, Bosses 3, Cards 21, lấy thẳng từ data thật, không có data mẫu) — vấn đề "gần như trống" chỉ là do trạng thái unlock thật (người chơi mới chưa khám phá gì). Thêm hằng số `DEV_UNLOCK_ALL = true` trong `CollectionManager.ts` (comment rõ TẠM THỜI, có hướng dẫn cách tắt) — ép lớp HIỂN THỊ show tất cả `unlocked: true`, không đụng logic ghi nhận unlock thật bên dưới. **Cần nhớ đổi lại `false` trước khi release** (đã ghi vào TASKS.md mục "Đang dở").
10. **Typography Collection + Boss Intro Cinematic** (2 việc độc lập, theo yêu cầu user, đã test qua Browser xác nhận):
    - **Typography**: phát hiện toàn bộ game (không riêng Collection) chưa từng set `fontFamily` ở đâu cả — mọi Text đang dùng font mặc định của Phaser (`Courier`, monospace), khó đọc, không hợp phong cách fantasy. Thêm `TITLE_FONT`/`BODY_FONT` (export từ `ui/CollectionCard.ts`, dùng chung cho `CollectionScene`/`CollectionTab`) — đều là font HỆ THỐNG có sẵn (Georgia/Trebuchet MS/Segoe UI), KHÔNG tải mạng/thêm dependency, không rủi ro FOUT trên canvas. Phân cấp: Title 16px (TITLE_FONT, stroke+shadow) > Badge/Rarity 9-11px > Stat/Description 12px (BODY_FONT, `lineSpacing` 3→6, màu mô tả sáng hơn `#9ca3af`→`#c7cad1` để tăng contrast). Chỉ sửa Collection (đúng scope yêu cầu), chưa đụng font toàn game.
    - **Boss Intro Cinematic**: xây `systems/BossIntroController.ts` (điều phối, không biết gì về Boss/GameScene internals, cùng tinh thần `VictoryController`) + `scenes/BossIntroScene.ts` (wrapper mỏng, launch song song). Khác `VictoryController` (chạy trong chính GameScene, chỉ hạ `timeScale`): ở đây GameScene bị **pause hẳn** (`scene.pause()`) vì hầu hết cooldown trong codebase (Boss skill, SpawnSystem...) so sánh theo raw `time`, `timeScale=0` không đủ để dừng — Boss AI tự "đứng yên" trong lúc pause vì `BossSystem.update()` chỉ được gọi từ `GameScene.update()`, không cần thêm cờ nào trên Boss. Toàn bộ cinematic (camera pan/zoom, shake, flash, overlay+vignette, Boss idle animation, typewriter) chạy trong `BossIntroScene` — scene SONG SONG không bị pause — và **tự tween trực tiếp `scrollX/scrollY/zoom`** của camera GameScene bằng `camera.centerOn()` mỗi tick, KHÔNG dùng `camera.pan()/zoomTo()` built-in (FX đó phụ thuộc update loop của scene sở hữu camera, không đảm bảo chạy khi scene đó đang pause). Cấu hình per-Boss qua data (`bosses.json`: `introText`/`introCameraZoom`), hằng số dùng chung ở `GameConfig.ts` `GAMEPLAY.BOSS_INTRO_*`. Typewriter có cursor nhấp nháy + skip bằng click/phím bất kỳ. GameScene chỉ còn trách nhiệm: spawn Boss entity (như cũ) → pause chính nó → launch `BossIntroScene` với data → nhận callback để emit `BOSS_SPAWNED` (dời từ lúc spawn sang lúc cinematic xong) + bật lại nút Pause — không nhúng logic cinematic nào vào GameScene/Boss. CHƯA có âm thanh (dự án chưa có SoundManager — xem TASKS.md "Đang dở").
11. **Boss Intro Cinematic v2 (state CUỐI CÙNG — đã qua 3 vòng chỉnh theo feedback user, gộp lại đây thay vì giữ từng bước trung gian)**: chi tiết kỹ thuật đầy đủ giờ nằm ở [BOSSES.md](../gameplay/BOSSES.md) mục "Boss Intro Cinematic" (đã viết thành module doc riêng, không lặp lại ở đây). Tóm tắt thay đổi so với bản v1 (state ban đầu, xem mục 10):
    - **Timing cố định 5000ms → 6500ms**, công thức `computeTypewriterTiming()` không đổi; bỏ field `introHoldMs` khỏi `BossDef` (hold time giờ luôn tính động theo ngân sách cố định, không còn override per-boss).
    - **Camera pan-in 1400ms → 3000ms** — tên Boss chỉ bung ra SAU khi camera dừng hẳn nên giờ xuất hiện đúng mốc ~3s (theo feedback user); `introText` cả 3 boss rút ngắn còn 1 câu (trước đó 2 vế nối "...").
    - **Vignette: Graphics vẽ tay (4 góc rồi 7 vòng đồng tâm) → Camera Post FX built-in** (`camera.postFX.addVignette()`, Phaser 3.60+, GPU) — mượt hơn hẳn, `Phaser.FX.Vignette.strength` tween trực tiếp được như object thường. Overlay đổi màu đen → xanh tím tối (`0x0d0614`) kết hợp cùng Vignette FX.
    - **Thêm mới**: hạt bụi/tro bay suốt cinematic, nhịp đập "thình...thình..." (heartbeat pulse: overlay+vignette đậm lên + camera rung nhẹ) trong giai đoạn hold, thay cho bản flicker ngẫu nhiên ban đầu (2 hiệu ứng cùng chỉnh alpha nên không thể chạy song song, chọn giữ heartbeat).
    - **Đã verify qua Browser** (nhiều lần, xuyên suốt 3 vòng chỉnh): console log `[BossIntro] <bossId>: ...ms` đo được 6533-6582ms (khớp mục tiêu 6500ms, chênh lệch giant_skeleton/grave_keeper chỉ ~2ms dù `introText` dài ngắn khác nhau — đúng tính chất công thức), bắt được hình ảnh cinematic giữa chừng (title pop, typewriter+cursor, vignette+particle) nhiều lần, resume gameplay chính xác sau đó (HP player không đổi trong lúc pause, Boss AI hoạt động lại), không lỗi console lần nào. Môi trường Browser pane có lúc bị throttle giữa chừng (rAF gần như đứng, không phải bug code — xem lại nếu cần tái test) nhưng không ảnh hưởng tới các lần verify thành công.
    - **CHƯA có âm thanh** (gõ chữ, nhạc nền) — dự án chưa có SoundManager, xem TASKS.md "Đang dở".
    - **Lưu ý còn treo, cần user xác nhận**: user từng nhắc chữ *"Giant Skeleton Xuất Hiện"* — đây là text của HUD banner có sẵn (`HUD.onBossSpawned()`), KHÁC với title cinematic "— GIANT SKELETON —" của `BossIntroController`. Banner đó chỉ hiện SAU KHI cinematic kết thúc hoàn toàn (event `BOSS_SPAWNED` dời sang lúc đó), KHÔNG phải ở mốc ~3s. Đã chỉnh đúng thời điểm title CINEMATIC (~3s) theo feedback, nhưng nếu ý user là muốn banner "Xuất Hiện" cũng đồng bộ về mốc đó thì cần làm thêm — CHƯA rõ, chưa sửa.
12. **Map Phase B — điền nội dung thật 8 map còn lại** (module doc đầy đủ giờ ở [MAP.md](../gameplay/MAP.md), bảng boss cập nhật ở [BOSSES.md](../gameplay/BOSSES.md); làm 3 batch theo yêu cầu user, dừng test sau mỗi batch, không có lỗi nào phát sinh giữa các batch):
    - **Batch 1 (map3-5)**: Swamp/Poison Hydra, Frozen Tundra/Frost Titan, Volcanic/Inferno Behemoth.
    - **Batch 2 (map6-8)**: Desert Ruins/Pharaoh's Curse, Corrupted Castle/Fallen King, Crystal Caves/Crystal Guardian.
    - **Batch 3 (map9-10)**: Sky Sanctum/Storm Dragon, Void Abyss/Soul Devourer (4 skill, map cuối cùng — mạnh nhất).
    - Mỗi map: 5 quái thường mới theo đúng 1 khuôn vai trò cố định (slime/basic-ground/phasing/tanky/flying — giữ đúng cấu trúc `EnemyDef` như `enemies.json`/`enemies_graveyard.json`), HP/damage giữ baseline gần Forest (KHÔNG tự nhân `difficultyMultiplier` vào data — multiplier đã áp dụng riêng lúc spawn trong `SpawnSystem`, giữ đúng 1 nguồn tính toán), chỉ khác tên/màu (`tintColor`) theo tông riêng biệt từng map. Boss mới `isFinalBoss: true`, tái dùng ĐÚNG 5 skill có sẵn (`dash`/`summon`/`ground_slam`/`charge`/`roar`) theo tổ hợp user chỉ định, HP tăng dần theo thứ tự map (2300 → 5200), đủ `introText`/`introCameraZoom` cho Boss Intro Cinematic.
    - **Gotcha bắt buộc khi thêm map mới**: mỗi file `enemies_*.json` mới PHẢI đăng ký thủ công vào `ENEMY_DATA_REGISTRY` trong `utils/MapData.ts` (Vite không hỗ trợ import động theo chuỗi runtime từ `maps.json.enemyDataFile`) — quên bước này thì map sẽ âm thầm fallback về quái Forest, không báo lỗi.
    - `maps.json` mỗi map: đổi `id`/`name`/`theme_color`/`enemyDataFile`/`bossId` khỏi placeholder Map3-10, GIỮ NGUYÊN `order`/`difficultyMultiplier`, sửa `unlockRequires` của CHÍNH map đó VÀ của map kế tiếp (đổi từ trỏ id cũ "map4"/"map5"... sang id mới) để không đứt chuỗi unlock tuần tự forest→...→void_abyss.
    - Đã test qua Browser sau mỗi batch (set `localStorage.soulhunter_cleared_maps` giả lập đã clear các map trước, mở `MapSelectScene`, xác nhận tên/màu/panel "Boss: ..." đúng, `npm run typecheck` sạch mỗi lần) — không còn "Map 3".."Map 10" placeholder nào, toàn bộ 10 map đã có nội dung thật.
13. **6 Boss Skill mới + Mid-Boss cho map3-10** (module doc đầy đủ giờ ở [BOSSES.md](../gameplay/BOSSES.md) mục "Boss Skills" + "Mid-Boss + Boss cuối"; làm theo đúng batch user yêu cầu — 2 skill batch, rồi wiring + 3 batch boss theo map — dừng test sau mỗi batch):
    - **Skill Batch 1**: Teleport (tự dịch chuyển quanh player + flash, hoàn toàn tự chứa trong `Boss.ts`), Meteor (TÁI DÙNG nguyên cờ `pendingSlamDamage` của Ground Slam, chỉ khác tâm AOE chốt tại vị trí player lúc bắt đầu cast thay vì vị trí boss), Freeze Pulse (xung mở rộng quanh boss, gọi `player.applySlow()` — field/method MỚI trên `Player`, cùng pattern `slowFactor/slowUntil` với `Enemy.applySlow()` sẵn có, `Player.update()` phải nhận thêm `time`).
    - **Skill Batch 2**: Poison Cloud (tạo `PoisonZone` sống ĐỘC LẬP trong `BossSystem`, không phụ thuộc Boss còn sống — tick damage player theo `tickIntervalMs` nếu đứng trong bán kính), Heal Self (PASSIVE theo ngưỡng HP, tách hẳn khỏi vòng xoay cooldown skill thường ngay từ `Boss` constructor, tự kiểm tra trong `takeDamage()`, cờ `healSelfUsed` chặn kích hoạt lần 2), Clone (TÁI DÙNG THẲNG `PoolManager.getEnemy()` với 1 `EnemyDef` tổng hợp runtime — AI đuổi theo/damage/bị giết/rơi Soul chạy MIỄN PHÍ qua `Enemy`/`CombatSystem`/`WeaponSystem` có sẵn, chỉ thêm cưỡng bức hết hạn qua mảng `activeClones` riêng).
    - **Wiring Mid-Boss + Boss cuối** (kiến trúc MỚI hoàn toàn — lúc user yêu cầu nhắc "xem cách Forest đang làm Giant Skeleton/Orc Warlord", đã xác nhận lại pattern đó THỰC RA CHƯA TỪNG TỒN TẠI, `orc_warlord` chưa gán map nào; tự xây từ đầu tái dùng tối đa `BossSystem.spawnBoss()`): `MapDef` thêm `midBossId?` (Forest/Graveyard không có), `GameScene` thêm `mapMidBossId`/`midBossSpawned` + `MID_BOSS_SPAWN_DEBUG_MS` (nửa `BOSS_SPAWN_DEBUG_MS`, cùng pattern debug cũ), `startBossIntro()` generalize nhận `bossId` param dùng chung cho cả 2 loại boss. Mid-Boss và boss cuối dùng CHUNG guard `!bossSystem.getBoss()` nên tự tuần tự, không cần thêm state machine riêng.
    - **Bug phát hiện lúc test, đã sửa**: Mid-Boss chết sát player (rất hay xảy ra, boss luôn đuổi cận chiến) → Loot Chest rơi đúng chỗ đó và có thể được nhặt NGAY trong cùng 1 frame `update()` → nếu elapsedPlayMs của boss cuối đã tới mốc, boss cuối spawn + tự pause GameScene (Boss Intro) NGAY TRONG CHÍNH FRAME ĐÓ, chồng lấn `BossLootScene` (vòng quay) với `BossIntroScene` (title cinematic) cùng lúc — bắt được qua screenshot lúc test Void Abyss (2 overlay đè lên nhau). Sửa bằng cờ mới `GameScene.lootFlowActive` (true từ lúc va chạm nhặt rương tới khi `BossLootScene` trả kết quả) — cả 2 điều kiện spawn Mid-Boss/boss cuối thêm check `!this.lootFlowActive`. Đã re-test lại Void Abyss xác nhận hết chồng overlay.
    - **8 Mid-Boss mới** (Bog Wraith/Ice Stalker/Magma Brute/Sand Reaper/Cursed Knight/Crystal Sentinel/Storm Harpy/Nightmare Herald, `isFinalBoss: false`, HP ~50% boss cuối cùng map) + **cập nhật `skillIds`** 8 boss cuối map3-10 theo đúng tổ hợp user chỉ định (Soul Devourer thêm Heal Self thành 5 skill, mạnh nhất game). Đã viết script Python đối chiếu **cả 19 boss KHÔNG trùng tổ hợp `skillIds`** (kể cả 3 boss cũ giant_skeleton/orc_warlord/grave_keeper) trước khi báo xong.
    - Đã verify bằng cách patch tạm `player.takeDamage`/`damageMultiplier` qua `window.__game` (expose tạm trong `main.ts`, revert ngay sau test) để test nhanh không cần chờ hàng phút — chơi thật Swamp (Bog Wraith→Poison Hydra→Victory→unlock Frozen Tundra), Frozen Tundra (Ice Stalker→Frost Titan), và Void Abyss (Nightmare Herald→Soul Devourer 5 skill→Victory, xác nhận toàn bộ 10 map đã "Đã chinh phục"). `npm run typecheck` sạch sau mỗi batch, mọi patch test tạm đã revert sạch (`git diff main.ts` rỗng).
14. **Fix BUG NGHIÊM TRỌNG: Level Up upgrade không có tác dụng thật** (user báo cụ thể HP Regen + Sword Range không thấy hiệu ứng, nghi ngờ lỗi hệ thống — đúng, audit toàn diện 21 upgrade trong `upgrades.json` phát hiện **14/21 hoàn toàn không hoạt động**, module doc đầy đủ giờ ở [UPGRADE.md](../gameplay/UPGRADE.md) mục "Cơ chế đọc/áp dụng từng stat"):
    - **Xác nhận event chain KHÔNG có vấn đề**: click card → `LevelUpScene.selectChoice()` → `UpgradeSystem.applyUpgrade(def)` chạy đúng, `player.stats[def.stat] = current + def.value` set giá trị ĐÚNG vào object. Bug KHÔNG nằm ở bước set — nằm ở việc **rất nhiều stat sau khi set xong không có bất kỳ code nào đọc lại**, vì `PlayerStats` có index signature `[key: string]: number` nên TypeScript/JS không hề báo lỗi khi ghi vào 1 field "vô danh" không ai dùng tới — false confidence kinh điển.
    - **14 upgrade BỊ ẢNH HƯỞNG (đã audit từng cái bằng grep tên field xuyên suốt codebase, không đoán mò)**: Move Speed (`moveSpeedMultiplier` — `Player.update()` chỉ dùng `stats.moveSpeed` thô), Critical Chance + Critical Damage (toàn bộ hệ thống crit CHƯA TỪNG được implement — không có bất kỳ đoạn roll nào dù `showDamageNumber()` đã có sẵn tham số `isCritical` chờ sẵn từ trước), Projectile +1 (`fire()` luôn chỉ bắn đúng 1 viên/mục tiêu), Fireball Size (`Projectile` không có cơ chế scale theo stat nào), Sword Range (`range` đọc thẳng `def.baseRange`, bỏ qua hẳn `swordRangeMultiplier` — đúng bug user báo), Poison (`poisonChance` không gate bất kỳ proc DOT nào), Life Steal (đọc NHẦM `WeaponDef.lifeStealPercent` — lifesteal riêng của 1 vũ khí fusion cụ thể — thay vì `player.stats.lifeStealPercent` mà upgrade này set), HP Regen (đúng bug user báo — không có tick nào cộng máu theo thời gian), Cooldown Reduction (`globalCooldownReduction` là stat riêng, tách biệt hẳn khỏi `cooldownMultiplier` đang hoạt động, chưa từng được đọc), Dodge Chance + Armor + Shield (cả 3 y hệt 1 dòng `// TODO: trừ shieldCharges trước, roll dodgeChance, trừ flatDamageReduction rồi mới trừ HP` bỏ ngỏ trong `Player.takeDamage()` — tính năng CHƯA TỪNG được viết, không phải bug set sai), Soul Value (`soulValueMultiplier` không nhân vào `enemy.def.soulValue` lúc rơi Soul).
    - **7 upgrade ĐÃ hoạt động đúng từ trước** (đối chiếu xong, không cần sửa): Damage, Attack Speed, Freeze Chance, Burn, Shrapnel, Magnet/Pickup Radius, Max HP (tăng trần đúng, chỉ thiếu cộng `currentHp` cùng lúc — đã bổ sung).
    - **Đã fix toàn bộ 14+1**: `Player.ts` (moveSpeedMultiplier áp vào tốc độ di chuyển; hp regen tick theo `delta` mỗi frame qua `heal()`; `takeDamage()` viết lại đúng thứ tự Shield→Dodge→Armor→trừ HP), `UpgradeSystem.ts` (`max_hp` cộng thêm `currentHp`), `WeaponSystem.ts` (crit roll áp dụng đều cho melee/projectile/random_target + truyền `isCrit` xuyên suốt `Projectile`/`applyDamage`/`applyDamageToBoss` để `showDamageNumber` hiện đúng; sword range nhân theo `def.id === "sword"`; fireball size truyền `scaleMultiplier` vào `Projectile.fire()` theo `def.id === "fireball"`; projectile+1 bắn thêm N viên dàn quạt (projectile-type) hoặc zap thêm N mục tiêu (random_target); poison proc dùng hằng số mới `GAMEPLAY.POISON_PROC_*`; life steal toàn cục gom về 1 điểm `applyGlobalLifeSteal()` áp cho MỌI nguồn damage; cooldown reduction cộng thêm vào công thức cooldown, sàn `COOLDOWN_REDUCTION_MIN_MULTIPLIER` chặn không cho về 0; soul value nhân vào `enemy.def.soulValue` trước khi spawn Soul/Dark Soul).
    - **Convention quan trọng phát hiện lúc fix, đã ghi vào UPGRADE.md**: các stat tên `...Multiplier` chia 2 nhóm khác hẳn nhau — nhóm ĐÃ pre-init trong `Player` constructor (damageMultiplier/cooldownMultiplier/critDamageMultiplier/pickupRadiusMultiplier, bắt đầu = 1) đọc THẲNG được; nhóm KHÔNG pre-init (moveSpeedMultiplier/fireballSizeMultiplier/swordRangeMultiplier/soulValueMultiplier) PHẢI tính `1 + (stat ?? 0)` tại nơi đọc, nếu đọc thẳng sẽ ra kết quả sai/NaN (đã tự kiểm tra kỹ, không đụng vào nhóm đã pre-init để tránh phá vỡ 7 upgrade đang chạy đúng).
    - **Đã verify qua Browser** bằng `window.__game` expose tạm (revert sau test): Sword Range — vòng chém phóng to gấp 3 lần thấy rõ trên màn hình khi set `swordRangeMultiplier=2`; HP Regen — đo bằng `performance.now()`, HP tăng từ 10→141/150 sau ~21s thực (phải tắt hẳn `equippedWeapons` trong lúc đo vì auto-kill liên tục gây level-up dồn dập làm GameScene pause giữa chừng, nhiễu phép đo); Shield — gọi `takeDamage(30)` trực tiếp 2 lần với `shieldCharges=2`, xác nhận HP không đổi + charges giảm đúng 2→1→0; Armor — sau khi hết shield, `flatDamageReduction=15` + damage 30 chỉ trừ đúng 15; Dodge — `dodgeChance=1` chặn 100% damage, `=0` nhận đủ. `npm run typecheck` sạch, đã revert sạch `main.ts` (không còn `window.__game` expose trong code thật).
15. **Vũ khí đặc biệt Holy Javelin** — đã có sẵn trong `data/weapons.json` từ sprint "final boss death sequence, loot wheel, daily login reward" (commit `38e7b5f`) nhưng chưa từng được ghi vào docs, bổ sung lại ở đây cho đúng trạng thái thật: `id: "holy_javelin"`, `type: "projectile_straight"`, 22 damage/1000ms cooldown, `rarity: "legendary"`, `locked: true` — chỉ xuất hiện trong pool roll của `UpgradeSystem.rollChoices()` SAU KHI đã unlock qua Daily Login Day 7 (`SaveData.claimLoginReward()` → `getCheapestLockedWeapon()` → `unlockWeapon()`) hoặc trúng ô "Unlock Weapon" trong Boss Loot Wheel — **KHÔNG có màn mua trực tiếp bằng Coin**, field `unlockCostCoin: 400` chỉ dùng để so sánh "vũ khí khóa rẻ nhất" khi hệ thống tự chọn ngẫu nhiên cho 2 cơ chế unlock trên (xem `WeaponDef.unlockCostCoin` comment trong `types/index.ts`). Cùng cơ chế `locked` với `storm_hammer` (2 vũ khí đặc biệt hiện có).
16. **Sprite nhân vật động theo characterId + Bow (Hunter) + Triple Throw (Assassin)** (module doc đầy đủ ở [ARCHITECTURE.md](../architecture/ARCHITECTURE.md) mục "Sprite nhân vật động theo characterId" + [WEAPONS.md](../gameplay/WEAPONS.md), làm qua nhiều batch theo yêu cầu user, dừng test sau mỗi batch):
    - **Load sprite động**: trước đó BootScene hardcode load cứng 1 bộ sprite Run/Attack cố định (thực chất luôn là art của "archer") cho MỌI nhân vật. Viết `utils/CharacterSpriteLoader.ts` load đúng theo `characterId` thật, key namespace `run_<characterId>_<dir>`/`attack_<characterId>_<dir>` để đổi nhân vật giữa phiên (không reload trang) vẫn đúng, không đè lẫn nhau. `BootScene.preload()` + `GameScene.preload()` (qua `init(data)`, vì Phaser không truyền data thẳng vào `preload()`) đều gọi loader này.
    - **Phát hiện mismatch tên thư mục thật vs characterId** lúc audit asset: `hunter` (Soul Hunter) dùng art nằm ở thư mục `archer/` (KHÁC nhân vật roster riêng `characterId: "archer"`, vũ khí Ice Shard, chưa có asset gì — dễ nhầm vì cùng tên gọi); `assassin` (2 chữ s) có thư mục đặt tên thiếu 1 chữ s thành `assasin`; `mage` chưa có subfolder `run/`/`attack/`, file nằm thẳng trong `player/mage/`. Verify qua Network requests thật (200 OK, không 404) sau khi user nghi ngờ bug đường dẫn — hoá ra code đã đúng từ đầu, khả năng cao là cache trình duyệt cũ lúc user test.
    - **Bow thay Sword cho Hunter**: đồng bộ đúng thời điểm mũi tên THẬT rời cung với animation Attack1 — release ở frame 3 (0-based, xác định bằng cách xem trực tiếp từng frame PNG, không đoán). Route qua sự kiện `animationupdate` DÙNG CHUNG của Phaser (verify trong source: `ANIMATION_UPDATE` không emit biến thể theo key như `ANIMATION_COMPLETE` — bug đầu tiên bắn ra 0 mũi tên vì nghe nhầm `animationupdate-<key>` không tồn tại). Quỹ đạo mũi tên luôn tính atan2 thật tới quái gần nhất, animation chỉ xấp xỉ 4 hướng gần nhất qua `aimAngleRad` — tách biệt hoàn toàn.
    - **Triple Throw thay Boomerang cho Assassin**: 3 dao bắn cùng lúc, lệch ±27° quanh quái gần nhất. Bản đầu dùng type mới `triple_spread_return` tái dùng cơ chế bay-xa-rồi-quay-lại của Boomerang, sau đó user yêu cầu bỏ hẳn cơ chế quay về — đổi thẳng `type: "projectile_straight"` (trúng 1 quái/hết `maxDistance` riêng 480 thì despawn, không pierce), gỡ toàn bộ state `returning` cho vũ khí này khỏi `Projectile.ts`. 1 bug thật phát hiện giữa chừng: animation Attack1 hiển thị sai hướng (theo `currentDirection` cũ từ WASD, KHÔNG phải hướng ném) dù quỹ đạo dao đã luôn đúng từ đầu — fix bằng cách truyền `aimAngleRad` vào `playAttackAnimation()` giống Bow.
    - **Fix bug chớp nháy animation lúc đứng yên (Assassin)**: KHÔNG phải do gọi lại `anims.play()` mỗi frame (đã guard sẵn) — là oscillation theo chu kỳ cooldown: Assassin chưa có Run, mỗi lần Triple Throw tự bắn xong rơi về `player_placeholder` làm Idle tạm, ~1.2s sau bắn tiếp hiện hình thật, lặp lại. Fix: thêm `Player.lastDirection`/`lastMovementState` (chỉ set lại animation khi 1 trong 2 đổi thật, dùng `null` đặc biệt để ép áp lại đúng 1 lần ngay sau khi Attack1 vừa xong) + đổi fallback ưu tiên frame đầu Attack (hình thật) thay vì rơi thẳng về placeholder.
    - **Trạng thái asset từng nhân vật** (xem bảng đầy đủ ở [ASSET_GUIDE.md](../assets/ASSET_GUIDE.md)): Hunter (archer/) đủ Run+Attack; Knight đủ Run+Attack, chưa có vũ khí riêng ngoài Sword; Assassin (assasin/) đủ Attack, thiếu Run (thư mục rỗng), đã có Triple Throw; Mage chỉ có Attack, thiếu Run; Archer (roster riêng) chưa có gì.
    - **Verify chủ yếu deterministic** (không phụ thuộc animation chạy theo thời gian thực — môi trường Browser pane test đã phát hiện lại vấn đề nhảy delta lớn khi tab mất focus, y hệt gotcha đã ghi ở ARCHITECTURE.md): fast-forward vị trí projectile rồi gọi `update()` 1 lần để check trigger đúng lúc; gọi `updateMovementAnimation()` nhiều lần liên tiếp với state cố định để đếm số lần thực sự set texture (10 lần gọi → 1 lần set, không phải 10). `npm run typecheck` sạch mỗi bước, mọi patch test tạm qua `window.__game` đã revert sạch.

## Việc cần theo dõi / nghi vấn bug (CHƯA xác nhận, CHƯA sửa)

- `PauseScene` loadout tray hiện badge `"Lv1"` cố định thay vì level thật của vũ khí đang trang bị — phát hiện lúc test tay Sprint UX, code phần này (`renderLoadoutTray`) không bị đụng trong sprint đó nên chưa rõ có phải bug thật hay hiểu nhầm lúc test. Cần user xác nhận trước khi ai đó sửa.
- HUD banner "XUẤT HIỆN" (`HUD.onBossSpawned()`) có nên đồng bộ về mốc ~3s (lúc camera tới Boss) giống title cinematic không, hay giữ nguyên hiện sau khi cinematic kết thúc hẳn — xem mục 11 ở trên, cần user xác nhận ý định trước khi sửa.
- Boss Intro Cinematic dùng `camera.postFX.addVignette()` — API này **chỉ chạy trên WebGL** (Phaser tự fallback im lặng nếu renderer là Canvas, không throw lỗi nhưng vignette sẽ không hiện). Project hiện chạy WebGL mặc định (`Phaser.AUTO` trong `GameConfig.ts`) nên chưa gặp vấn đề, nhưng CHƯA test trên nhiều trình duyệt/thiết bị khác nhau (đặc biệt các trình duyệt/di động cũ có thể fallback Canvas) — xem TASKS.md.

## Nợ tài liệu (docs/gameplay chưa cập nhật theo code mới nhất)

`TASKS.md` đã có mục cho các sprint trên, nhưng CHƯA có module doc riêng (kiểu `WEAPONS.md`/`BOSSES.md`) cho: VictoryController/VictoryScene, Boss HP UI, Card redesign, Weapon HUD tray. Muốn hiểu chi tiết kỹ thuật các phần này, đọc thẳng code (`GameScene.ts`, `VictoryController.ts`, `VictoryScene.ts`, `HUD.ts`, `WeaponIcon.ts`) thay vì tìm trong `docs/gameplay/`, cập nhật `UI.md` hiện có nếu cần. Riêng **Map Selection System + 10 map thật (Phase B) đã có doc đầy đủ** ở [MAP.md](../gameplay/MAP.md) (bảng danh sách map/boss/data file), và **Boss Intro Cinematic đã có doc đầy đủ** ở [BOSSES.md](../gameplay/BOSSES.md) (mục mới, xem trên — bảng boss cũng đã cập nhật đủ 11 boss). `BOSSES.md` mục "Final Boss death sequence" (cutscene 3 bước bubble effect) đang mô tả implementation CŨ, đã bị thay bằng `VictoryController.ts` (Zoom → Slow Motion 25% → Boss tan biến, xem mục "Victory Flow" ở sprint 2) — chưa cập nhật lại vì ngoài phạm vi các yêu cầu gần đây, cần dọn riêng.

**Liên kết:** [TASKS.md](./TASKS.md), [ROADMAP.md](./ROADMAP.md), [CLAUDE.md](../../CLAUDE.md)
